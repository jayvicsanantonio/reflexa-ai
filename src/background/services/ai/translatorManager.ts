/**
 * Translator Manager - Re-export for backward compatibility
 */

export {
  TranslatorManager,
  translatorManager,
  SUPPORTED_LANGUAGES,
} from './translator';
export type { ITranslatorManager } from './translator';
