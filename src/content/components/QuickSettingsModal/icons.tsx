/**
 * Quick Settings Modal Icons
 * Shared SVG icons used in the Quick Settings Modal
 */

import {
  Volume2,
  Move,
  FileCheck,
  Languages,
  FlaskConical,
  Clock,
  List,
  Database,
  Mic,
  Settings,
} from 'lucide-react';

export const IconSound = <Volume2 size={16} strokeWidth={2} aria-hidden />;

export const IconMotion = <Move size={16} strokeWidth={2} aria-hidden />;

export const IconProofread = (
  <FileCheck size={16} strokeWidth={2} aria-hidden />
);

export const IconTranslate = (
  <Languages size={16} strokeWidth={2} aria-hidden />
);

export const IconBeaker = (
  <FlaskConical size={16} strokeWidth={2} aria-hidden />
);

export const IconClock = <Clock size={16} strokeWidth={2} aria-hidden />;

export const IconList = <List size={16} strokeWidth={2} aria-hidden />;

export const IconDatabase = <Database size={16} strokeWidth={2} aria-hidden />;

export const IconMic = <Mic size={16} strokeWidth={2} aria-hidden />;

export const IconSettings = <Settings size={20} strokeWidth={2} aria-hidden />;
