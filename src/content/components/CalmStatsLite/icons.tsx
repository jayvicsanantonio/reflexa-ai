/**
 * CalmStatsLite Icons
 * SVG icons used in CalmStatsLite component
 */

import { Calendar, Book, Heart } from 'lucide-react';

export const IconCalendar = <Calendar size={18} strokeWidth={2} aria-hidden />;

export const IconBook = <Book size={12} strokeWidth={2} aria-hidden />;

export const IconHeart = <Heart size={12} strokeWidth={2} aria-hidden />;
