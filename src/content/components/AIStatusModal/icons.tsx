/**
 * AI Status Modal Icons
 * Shared SVG icons used in the AI Status Modal
 */

import {
  List,
  PenTool,
  Repeat,
  BookCheck,
  Globe,
  Languages,
  Zap,
} from 'lucide-react';

export const IconList = <List size={16} strokeWidth={2} aria-hidden />;

export const IconPen = <PenTool size={16} strokeWidth={2} aria-hidden />;

export const IconRepeat = <Repeat size={16} strokeWidth={2} aria-hidden />;

export const IconBookCheck = (
  <BookCheck size={16} strokeWidth={2} aria-hidden />
);

export const IconGlobe = <Globe size={16} strokeWidth={2} aria-hidden />;

export const IconTranslate = (
  <Languages size={16} strokeWidth={2} aria-hidden />
);

export const IconSpark = <Zap size={16} strokeWidth={2} aria-hidden />;
