import './styles.css';

console.log('Reflexa AI content script initialized');

// Content script orchestration will be implemented in later tasks
// This file will handle:
// - Dwell time tracking
// - Content extraction
// - UI injection via shadow DOM
// - Communication with background worker

// Placeholder for future implementation
const initializeContentScript = () => {
  console.log('Content script ready');

  // CSS is automatically injected by Vite build process
  // Shadow DOM will be used for UI injection in later tasks
};

initializeContentScript();
