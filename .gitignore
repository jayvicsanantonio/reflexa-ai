# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build output
build
*.zip

# Test coverage
coverage
*.lcov

# Vite cache
.vite
dist/.vite
node_modules/.vite

# Environment variables
.env
.env.local
.env.*.local

# Lock files (keep only one based on package manager)
# Project uses npm, so ignore pnpm and yarn locks
pnpm-lock.yaml
yarn.lock

# Husky (git hooks are tracked, but _ directory is cache)
.husky/_

